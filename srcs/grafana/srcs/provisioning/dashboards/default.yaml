apiVersion: 1

providers:
  - name: Default
    folder: Services  # The folder where to place the dashboards
    type: file
    options:
      path: /var/lib/grafana/dashboards # path to dashboard definitions (in my case, the one from the sample dashboard config)